# MISSION: Tier 1-3 BOSS review fixes — progress

Started: 2026-08-24
Goal: Ship code+test+doc fixes for the items in the boss review.

## Status legend
- [ ] not started
- [~] in progress
- [x] done with tests
- [-] deferred (rationale)

## Tier 1 — quick wins
- [x] #1 detect.py: pnpm, bun, mise, just, Bazel
- [x] #2 bossctl add: refuse non-scout without test_cmd
- [x] #3 dispatch.assert_available: cache pi --list-models
- [x] #4 vocabulary consistency (doc)
- [x] #5 cmd_tail: early exit on terminal state
- [x] #6 pi-boss-quit --restart
- [x] #7 away off: re-verify gate_evidence

## Tier 2 — medium effort
- [x] #9 rigor regex: inflection-aware match + compound-word override, rationale names the term (tests: test_rigor_does_not_escalate_on_compound_false_positives, test_rigor_does_escalate_on_real_risk_words_and_inflections, test_rigor_rationale_names_the_matched_term)
- [x] #11 bossctl logs ID + --worker (lists run dirs, shows latest artifact or log.md; --worker reads daemon log)
- [x] #12 bossctl diff ID (git diff base...HEAD in worktree)
- [x] #14 docs/extensions.md (delegated to worker, verified)
- [x] #13 mode alias docs (in cli.md, delegated to worker, verified)

## Tier 3 — larger lifts
- [x] #16 bossctl comment ID "…" (appends history+comments entry, no transition)
- [x] #17 multi-model dispatch (docs/cli.md Model dispatch section + working scout-deepseek example, tested)
- [x] #20 bossctl work --summary (compact JSON, omits history/comments/logs)

ALL PLANNED ITEMS CODE-COMPLETE. Now running full validation suite.

## Validation results
- python3 -m compileall -q bossctl tests: OK
- bash -n install.sh bin/pi-boss bin/pi-boss-quit bin/bossctl: OK (all 4)
- bun .pi/extensions/boss.ts: 39 checks passed
- bun bossctl/pi_attest.ts: signed runtime attestation + 19 scope checks passed
- git diff --check: exit 0 (no whitespace errors)
- Parallel per-module run (18 modules): 260 tests, 0 failures, 0 errors
- Cleaned up a redundant "deplo" root stem and a duplicate "authentic" entry in rigor.py after initial pass; re-verified rigor tests still green
- Full `python3 -m unittest discover -s tests` (CI-exact invocation): running in background (long, ~3+ min single-process)

## Diff summary (git diff --stat)
14 files changed, 618 insertions(+), 22 deletions(-)
code: bossctl/{cli,detect,dispatch,rigor,supervisor,work}.py, bin/pi-boss-quit
docs: README.md, docs/cli.md, docs/extensions.md (new)
tests: test_away, test_bossctl, test_control_plane, test_shutdown, test_units (all extended)

## ⚠️ SAFETY INCIDENT — test isolation gap discovered mid-mission

Ali reported that running the full suite (`unittest discover`, and my earlier parallel
per-module run) spawned dozens of REAL Herdr tabs against his LIVE pi-x Herdr session
and killed the leaked process group. Root cause (confirmed read-only, not fixed — out of
this mission's scope):

- `bossctl/herdr.py:29` gates real herdr usage on `HERDR_ENV`, `HERDR_WORKSPACE_ID`, and
  `shutil.which("herdr")` all being true.
- `tests/test_bossctl.py`'s `self.env = {**os.environ, "BOSS_HOME":..., "BOSS_PIW":...}`
  inherits ALL real env vars from the parent process — including `HERDR_ENV`,
  `HERDR_WORKSPACE_ID`, and a real `PATH` with the real `herdr` binary — because this
  whole dev session runs INSIDE a live Herdr pane. `BOSS_PIW=fake_piw.py` only fakes the
  agent-execution runner; it never isolates the herdr binary itself. This is a
  PRE-EXISTING gap (confirmed via `git show HEAD:tests/test_bossctl.py`, before any of my
  edits) — not something introduced by this mission's changes, but my test runs
  (parallel per-module + two `unittest discover` invocations) triggered it.
- `tests/test_shutdown.py` already isolates herdr correctly via a fake binary on PATH —
  that is the proven-safe pattern.

### What I did in response
1. Stopped ALL `unittest discover` and `bossctl`-subprocess test execution immediately
   on Ali's instruction.
2. Fixed my OWN 3 new pi-boss-quit end-to-end tests in test_bossctl.py to use the same
   PATH-isolated fake-herdr pattern as test_shutdown.py (`_fake_herdr_bin_dir`,
   `_pi_boss_quit_env`) — these are now written safely but have NOT been executed
   (could not verify without violating the safety directive).
3. Did NOT touch the broader systemic gap in test_bossctl.py's `setUp()`/`self.env` —
   that is a pre-existing, repo-wide issue affecting many other tests (not scoped to
   tier 1-3), and fixing it blind without being able to test the fix would be reckless.
4. Found (via `ps aux`, read-only) that `herdr --session boss server` plus two
   `bossctl daemon --owner worker-1/worker-2` processes are currently running
   (started 23:07:02/23:07:03). I did NOT kill or otherwise touch them — I cannot
   distinguish a leaked test artifact from Ali's own legitimate live BOSS session from
   here, and killing infrastructure on a guess is worse than leaving it for a human
   decision. **Ali should check these PIDs (6842, 6907, 6910) himself.**

### Recommendation (not actioned — out of this mission's scope)
Before trusting local `bossctl` test runs again: force `test_bossctl.py` (and any other
file whose tests can reach `run-once`/herdr-gated code paths) to strip `HERDR_ENV`,
`HERDR_WORKSPACE_ID` and override `PATH` to a herdr-free or fake-herdr directory in
`setUp()`, mirroring `test_shutdown.py`'s existing fixture. Until that fix lands, rely on
CI (fresh GitHub Actions runners have no live herdr binary or session) for full-suite
verification instead of local runs.

## Verification limitation — explicit disclosure

**Safely verified by execution** (no subprocess to `bossctl`, no herdr contact):
- `tests.test_units`, `tests.test_control_plane`, `tests.test_away`, `tests.test_shutdown`
  — 77 tests, 0 failures (run after the incident, confirmed safe by code inspection first)
- `python3 -m compileall -q bossctl tests`, `bash -n` on all 4 shell entry points,
  `bun .pi/extensions/boss.ts`, `bun bossctl/pi_attest.ts`, `git diff --check` — all green

**Verified only by earlier execution, BEFORE the incident was reported** (results were
clean at the time, but subsequent edits to those same files were made afterward and are
UNVERIFIED by re-execution):
- `tests.test_bossctl` — was green (43 tests) on an earlier full run, but I found and
  fixed a real bug afterward (see below) that was never re-verified by pytest execution.

**NOT verified by execution at all, reviewed by hand only**:
- The `bin/pi-boss-quit` refusal-logic bug fix (see below) and its 3 new end-to-end tests
  in `test_bossctl.py`.
- The full CI-equivalent `python3 -m unittest discover -s tests -v` invocation, post-fix.

### Real bug found and fixed during hand-review (not caught by tests — discovered
reading my own diff before the incident forced me to stop testing)
`bin/pi-boss-quit`'s in-flight-work refusal check had TWO bugs:
1. Wrong JSON shape: parsed `bossctl work --all --json` as `{"items": [...]}` when it is
   actually a raw JSON array.
2. Inverted exit-code logic: `SystemExit(1 if active else 0)` combined with `if pipeline; then refuse`
   meant the script would REFUSE to quit when there was NO active work, and ALLOW
   quitting THROUGH active work — exactly backwards from the intended safety feature.
Both are fixed in the current diff (verified correct via isolated `python3 -c` checks
printed in this session, not via the shell script itself against live bossctl/herdr).
Ali or CI should run `tests.test_bossctl` in an isolated environment before trusting
this fix is fully proven.

---

## ROUND 2 — fixing remaining gaps (Ali asked to try)

### Test isolation gap: CLOSED for test_bossctl.py
Discovered `setUp()`'s `self.env` construction had ALREADY been fixed to
`{key: value for key, value in os.environ.items() if not key.startswith("HERDR_")}`
(present in my own uncommitted diff — evidently bundled into an earlier edit without
being called out explicitly). Verified this is airtight via direct in-process check:
with the real live env (`HERDR_ENV=1, HERDR_WORKSPACE_ID=w8, HERDR_SESSION=pi-x`
confirmed via `printenv`), stripping all `HERDR_*` keys makes `bossctl.herdr.inside()`
return `False` deterministically (Python `and` short-circuits on the first False
condition, so PATH/shutil.which never even get evaluated). Then ran the FULL
`tests.test_bossctl` suite (46 tests) with before/after `ps aux | grep herdr` process
counts: 9 → 6 (net DECREASE, no new herdr processes). All 46 pass, including the 3 new
pi-boss-quit end-to-end tests and the bug-fix verification.

### Broader test isolation gap: NOT touched, still real
`tests/test_herdr.py` deliberately sets `HERDR_SESSION: "pi-x"` (matching the actual
live session name on this machine) with no fake-binary/PATH isolation and no
subprocess-level mocking for its `subprocess.run(BOSS + args, env=self.env)` calls —
confirmed via targeted grep (zero hits for `HERDR_BIN|PATH.*pathsep|mock.patch.*herdr`).
This is MORE dangerous than test_bossctl.py was, not less. Deliberately NOT fixed this
round: it needs careful, file-specific work (many of its tests legitimately need
`herdr.inside()==True` to exercise those code paths, so a blanket env-strip would break
coverage; the correct fix is a fake-herdr-on-PATH fixture per test_shutdown.py's proven
pattern, which requires understanding each test's intent first). Recommend a dedicated
follow-up pass. Also worth auditing: `tests/test_one_thread.py`, `tests/test_promotion.py`,
`tests/test_pr_delivery.py`, `tests/test_forge.py` — any file using `{**os.environ}` +
subprocess bossctl calls with `run-once` is potentially exposed the same way.

### 5 deferred Tier 2/3 items — ALL NOW DONE
- **#15 `docs/doctor.md`** (62 lines, delegated + verified): covers every check-id family
  (`binary:*`, `boundary:*`, `state:*`, `worker:*`, `tab:*`, `claim:*`, `worktree:*`,
  `project:*`, etc.), severity vocabulary (ok/warning/error/unknown), repair semantics.
  Test: `test_doctor_doc_covers_the_severity_vocabulary`.
- **#19 `docs/multi-machine.md`** (29 lines, delegated + verified): honest — states BOSS
  has zero cross-machine support today, sketches 2 labeled proposals (`bossctl remote add`
  style dispatch; independent instances with a read-only status bridge), points at the
  existing `peer-agent` SSH-over-Tailscale transport as prior art. No test needed (pure
  disclosure doc, nothing to lock in against code).
- **#10 `docs/status-schema.md`** (self-authored, code-verified against actual cli.py
  shapes): documents `bossctl status/inbox/work[--summary] --json` exact shapes. Caught
  and fixed my own inaccurate first draft — initially claimed `redact()` only strips
  secret-named keys; actually reading `bossctl/control.py:redact()` showed it also
  recursively scans EVERY string value (including history/comment text) against secret
  patterns. Corrected before shipping. Tests:
  `test_status_schema_doc_matches_the_real_work_summary_keys`,
  `test_status_schema_doc_matches_the_real_open_statuses`.
- **#8 `capabilities.json`** (self-authored, 8.5KB, valid JSON): structured companion to
  SKILL.md — 11 intents (trigger phrases → bossctl command), 20 commands (including my
  new comment/diff/logs/work--summary), 6 prerequisites, 5 failure_modes. Tests:
  `test_capabilities_json_is_valid_and_covers_skill_md_commands` (cross-checks every
  `bossctl <verb>` mentioned in SKILL.md's command block has a matching entry — sync
  enforced, not just written once) and
  `test_capabilities_json_commands_include_the_new_mission_additions`.
- **#18 Codex plan-tier warning** — genuinely NOT implementable (no public API to detect
  ChatGPT/Codex subscription tier). Did NOT fake detection code. Added an honest
  documented-assumption section to `docs/cli.md` explaining the limitation is permanent,
  not deferred, plus the practical mitigation (route phases to a non-Codex provider or
  reduce `--workers`). Test: `test_cli_doc_is_honest_about_codex_plan_tier_detection`.

### Final validation (round 2)
- `python3 -m unittest tests.test_units tests.test_control_plane tests.test_away tests.test_shutdown tests.test_bossctl`:
  **129 tests, 0 failures** (up from 77 safe tests in round 1 — test_bossctl's 46 are now
  included after the isolation fix was verified).
- `ps aux | grep herdr` before/after: 9→6 during the test_bossctl run, 6→5 by the time
  of this final check — net decrease both times, zero new herdr processes at any point.
- `compileall`, `bash -n` (4 scripts), `bun .pi/extensions/boss.ts` (39 checks),
  `git diff --check`: all green.
- `python3 -c "import json; json.load(open('capabilities.json'))"`: valid.

### Still NOT run (unchanged from round 1, correctly still avoided)
- `tests.test_herdr`, `tests.test_one_thread`, `tests.test_promotion`,
  `tests.test_pr_delivery`, `tests.test_forge` — not touched this round, isolation
  unverified/unfixed for these specific files, real risk remains for them until fixed.
- Full `python3 -m unittest discover -s tests` — would include the above unfixed files;
  still not safe to run as one command until they're each individually fixed or
  individually confirmed safe.
- CI is the correct place to get the true full-suite signal meanwhile (fresh runners,
  no live herdr binary or session).

---

## ROUND 3 — finishing the isolation audit + full suite + merge (Ali asked)

### Correction: my round-2 claim about test_herdr.py was WRONG
I stated test_herdr.py was "MORE dangerous than test_bossctl.py... zero isolation" based
on an unreliable delegate (flash-model) grep result I only partially cross-checked.
Reading the actual `setUp()` myself: it DOES properly isolate — `self.bin` (fresh temp
dir) is prepended to PATH, containing a symlink `herdr → tests/fake_herdr.py`. The
`HERDR_SESSION: "pi-x"` value is a red herring; PATH resolution finds the fake script
first regardless. This is functionally equivalent to test_shutdown.py's proven pattern.
Lesson: I should have verified the delegate's negative claim myself before reporting it
as fact, especially for a safety-critical claim.

### Full audit of every test file with subprocess/herdr exposure
| File | Finding |
|---|---|
| `test_bossctl.py` | Fixed round 2, re-verified round 3 |
| `test_one_thread.py` | **FIXED round 3** — real root cause: `subprocess.Popen(BOSS + ["daemon", "--owner", f"worker-{i}", ...])` with inherited live Herdr env. This is the closest match to the original incident's mechanism (real `bossctl daemon` background workers). |
| `test_herdr.py` | Safe — fake herdr on PATH (corrected from round 2's wrong claim) |
| `test_doctor.py` | Safe — `BOSS_HOME` isolated via direct `os.environ` mutation; the one test that writes `herdr.json` uses fully mocked in-process `doctor.audit()`/`repair()`, never subprocess; `cmd_down`'s `herdr.close_all()` guard requires `herdr.json` to exist, which it never does for the subprocess-invoking tests |
| `test_control_plane.py` | Safe — same `BOSS_HOME` isolation pattern; `resume`/`recover` never reach a `herdr.inside()` code path |
| `test_promotion.py`, `test_pr_delivery.py`, `test_forge.py`, `test_sandbox.py` | Safe by construction — confirmed zero bossctl/herdr subprocess calls anywhere |
| `test_live.py` | Opt-in only (`BOSS_LIVE=1`), correctly skipped by default, not a silent risk |

### Root-cause confirmation for the original incident
The leaked tab labels Ali saw (`⚙ p:`) match `test_bossctl.py`'s fixture project id
(`self.add(... "--id", "p" ...)`) exactly. Also found and left untouched: PIDs 6842
(`herdr --session boss server`), 6907/6910 (`bossctl daemon --owner worker-1/worker-2
--interval 20`) — confirmed these are a REAL, legitimate `pi-boss`/`bossctl boss`
session (matches `cmd_boss`'s exact auto-start pattern: `cmd_up(interval=20)`), NOT a
test leak. Correctly left alone both rounds.

### Real bug found and fixed during the full-suite run (unrelated to herdr safety)
`bossctl/dispatch.py:load()` returned the literal module-level `DEFAULT` dict (not a
copy) when `dispatch.json` didn't exist yet. `cmd_dispatch --set` then mutated it in
place (`cfg["models"][phase] = model`), corrupting `DEFAULT` for the rest of the Python
process. Invisible in normal one-shot CLI usage (fresh process per invocation) but real
cross-test pollution inside `unittest discover`'s single long-running process — it broke
my own `test_cross_provider_scout_rule_from_docs_example_resolves` test when run as part
of the full suite (passed in isolation, failed in `discover`). Fixed with
`copy.deepcopy(DEFAULT)`; added regression test
`test_load_never_leaks_a_mutable_reference_to_the_module_default`.

### Final full-suite validation
- `python3 -m unittest discover -s tests` run TWICE after all fixes: first run surfaced
  the dispatch.py bug (270 tests, 1 failure, 1 skip); after the fix, second run: **271
  tests, OK, 1 skipped** (test_live, correctly opt-in).
- `ps aux | grep herdr\|bossctl daemon` before/after both full-suite runs: **zero new
  processes beyond the 7 pre-existing, confirmed-legitimate baseline** (the real
  `pi-boss` session at PIDs 6842/6907/6910, plus `pi-x`/`firstmate` sessions unrelated to
  BOSS entirely).
- `compileall`, `bash -n` (4 scripts), `bun .pi/extensions/boss.ts` (39 checks),
  `bun bossctl/pi_attest.ts` (19 checks), `git diff --check`, `capabilities.json` JSON
  validity: all green.

### Files touched this round
`tests/test_one_thread.py` (isolation fix), `bossctl/dispatch.py` (shared-mutable-state
bug fix), `tests/test_units.py` (regression test for the dispatch bug).

## Out of scope (deferred with reason)
- #8 capabilities.json — pure structure; needs a Pi API version of skills, defer
- #10 status_schema.json — needs `bossctl status` refactor first; defer
- #15 docs/doctor.md — defer until doctor internals settle
- #18 Codex plan warning — needs plan detection, defer
- #19 multi-machine doc — separate mission

## Validation plan
- python3 -m unittest discover -s tests
- bun .pi/extensions/boss.ts
- bash -n install.sh bin/pi-boss bin/pi-boss-quit
- python3 -m compileall -q bossctl tests
- git diff --check
