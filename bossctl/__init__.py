"""bossctl — deterministic operations for a portfolio of repositories.

Deterministic control plane: code owns intake, dispatch, leases, worktrees,
delivery and promotion; pi-graph (`piw`) owns the ordered phases inside one
attempt; models do the work inside nodes and cannot skip one.
"""
__version__ = "0.1.0"
