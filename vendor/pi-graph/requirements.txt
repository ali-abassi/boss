# Runtime dependencies for the piw CLI and workflow runner.
# Kept in sync with pyproject.toml and install.sh.
PyYAML>=6,<7
ruamel.yaml>=0.18,<0.19
jsonschema>=4.23,<5
# BOSS verifies the Ed25519 runtime/event receipts emitted by managed Pi
# sessions.  This is a runtime dependency, not an optional test extra.
cryptography>=42,<51
